var fs = require('fs')
var objects = require('./objects')

function AddRef(document, refname) {
    if (fs.existsSync(refname)) {
        document.setresponsehandler(function() {
            document.GetRawResponse().sendFile(`${process.cwd()}/${refname}`)
        }, `/${refname}`)
    }
}

module.exports = AddRef